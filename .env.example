# .env.example (template)

# git_token: GitHub Personal Access Token. Set this to your PAT.
# Must have Contents (read/write) and Pull requests (read/write) scopes.
git_token=ghp_your_personal_access_token_here

# openai_api_key: OpenAI API Key for LLM calls.
openai_api_key=sk_your_openai_api_key_here

# github_repository: GitHub repository in 'owner/repo' format (e.g., your-org/your-repo).
github_repository=your-org/your-repo

# Copy this file to .env and replace the placeholders with your actual values.
# Do not commit the .env file.